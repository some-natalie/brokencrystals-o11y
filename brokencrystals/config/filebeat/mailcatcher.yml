filebeat.inputs:
  - type: filestream
    id: mailcatcher-container-logs
    paths:
      - '/var/lib/docker/containers/*/*.log'
    parsers:
      - container:
          stream: all
          format: auto
    processors:
      - add_docker_metadata:
          host: 'unix:///var/run/docker.sock'
      - add_fields:
          target: ''
          fields:
            service.name: mailcatcher
            service.type: mail

output.elasticsearch:
  hosts: ['http://host.docker.internal:9200']
  username: 'elastic'
  password: 'Tn99rhPs'
  index: 'filebeat-mailcatcher-%{+yyyy.MM.dd}'

setup.ilm.enabled: false
setup.template.name: 'filebeat-mailcatcher'
setup.template.pattern: 'filebeat-mailcatcher-*'

logging.level: info
logging.to_files: true
logging.files:
  path: /var/log/filebeat
  name: filebeat
  keepfiles: 7
  permissions: 0644
